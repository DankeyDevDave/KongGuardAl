apiVersion: 1

datasources:
  # Direct connection to AI services (no Prometheus needed)
  - name: Kong-AI-Cloud-Direct
    type: prometheus
    access: proxy
    url: http://host.docker.internal:18002
    isDefault: true
    editable: true

  - name: Kong-AI-Ollama-Direct
    type: prometheus
    access: proxy
    url: http://host.docker.internal:18003
    editable: true

  # Optional: Connect to production Prometheus
  - name: Kong-Production-Prometheus
    type: prometheus
    access: proxy
    url: http://198.51.100.225:9090
    editable: true
